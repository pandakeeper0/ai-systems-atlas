# Inbox

Fast capture area for material that is interesting but not yet processed.

Use this when speed matters more than structure. Items should eventually be deleted, moved into `sources/`, or synthesized into `topics/`.
